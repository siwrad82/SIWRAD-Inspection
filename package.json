{
  "name": "siwrad-inspection",
  "version": "1.0.0",
  "description": "SIWRAD Inspection Android Application",
  "author": "SIWRAD Engineering",
  "license": "MIT",
  "private": true,
  "scripts": {
    "prepare": "mkdir -p www && cp -r src/* www/",
    "build": "npm run prepare",
    "sync": "npx cap sync android",
    "android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/camera": "^8.2.1",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
